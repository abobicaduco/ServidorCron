__pycache__/
*.py[cod]
*.log
.env
.venv/
venv/
.env.local

# Enterprise-only copies (internal branding, paths, or confidential strings — never push)
ServidorCron.py
ServidorCron.html

# Windows CMD quirk: `2>$null` creates a literal file named "$null" (use PowerShell or `2>nul` in CMD)
$null

# Local access / optional Excel (may contain names — keep private)
access_registry.xlsx
optional_cobranca_cron.xlsx

# App data (Windows)
**/AbobiServerCron/logs/
**/jobs_scheduler.sqlite

# User home state
.abobi-server-cron/

# IDE
.idea/
.vscode/
*.swp
